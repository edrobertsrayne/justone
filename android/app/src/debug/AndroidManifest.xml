<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Debug-only: the Firebase Emulator Suite speaks plain HTTP, which Android
         blocks by default. Permit cleartext so debug builds can reach the emulator
         (e.g. tool/seed_emulator.dart). Release builds keep cleartext disabled. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
